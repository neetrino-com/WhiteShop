# 🚀 Інструкції для запуску проекту

## 📋 Кроки для запуску:

### 1️⃣ Запустіть MongoDB
```bash
# Перевірте, чи MongoDB запущений
# Якщо використовуєте Docker:
docker ps | findstr mongo
```

### 2️⃣ Заповніть базу даних (якщо ще не зроблено)
```bash
cd ../api
node src/seed.js
```

Це створить:
- 2 користувачів (admin та test user)
- 4 бренди (Apple, Samsung, Nike, Adidas)
- 6 категорій
- 9 продуктів з варіантами

### 3️⃣ Запустіть API сервер
```bash
cd ../api
npm start
# або
node src/server.js
```

API буде доступний на: `http://localhost:3001`

### 4️⃣ Запустіть Frontend сервер
```bash
cd ../web
npm run dev
```

Frontend буде доступний на: `http://localhost:3000`

## ✅ Перевірка:

1. **API Health Check:**
   - Відкрийте: `http://localhost:3001/health`
   - Має повернути: `{"status":"ok"}`

2. **API Products:**
   - Відкрийте: `http://localhost:3001/api/v1/products`
   - Має повернути список продуктів

3. **Frontend:**
   - Відкрийте: `http://localhost:3000`
   - Має відобразитися головна сторінка з продуктами

## 🔧 Якщо є проблеми:

### ChunkLoadError:
```bash
npm run dev:clean
```

### API не підключається:
- Перевірте, чи API сервер запущений на порту 3001
- Перевірте змінну `NEXT_PUBLIC_API_URL` в `.env`

### База даних порожня:
```bash
cd ../api
node src/seed.js
```

